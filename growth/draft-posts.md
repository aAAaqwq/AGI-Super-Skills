# Human-reviewed draft posts

These are editing prompts, not approved copy. Replace brackets with verified facts, run G4, adapt to the community, and post manually. Delete any sentence you cannot support.

## Safe preview

> Installing agent bundles should not begin with surprise writes. AGI Super Team’s local installer now previews by default; `--apply` is a separate step. We tested `[revision]` with `[commands]` in `[environment]`. Known limit: `[limit]`. Walkthrough: `[main-branch link]`. I contribute to this project and would value feedback on `[specific question]`.

## Codex package

> We separated the curated Codex-native package from the generic starter kits so the install and update boundaries are explicit. At `[revision]`, the package includes `[validator-backed description, not a guessed count]`. Provenance and safe sync behavior: `[main-branch link]`. I contribute to the project; what would you need to reproduce this safely?

## Starter-kit evaluation

> We evaluated the `[kit]` starter kit on one bounded task: `[task]`. The result was `[observable result]`; it did not test `[limitations]` and made no external changes. Reproduction steps: `[main-branch link]`. This is not a claim of production readiness. Which failure case should the next evaluation cover?

## Reddit-specific disclosure

Add near the first project link: “Disclosure: I contribute to AGI Super Team.” Keep the useful explanation in the post; do not force readers to click for basic context.

## Correction reply

> Thanks for flagging this. The claim `[old claim]` was not adequately supported by `[evidence gap]`. We corrected `[location]` at `[revision/link]` and recorded the change. The narrower supported statement is `[new claim]`.
